// Simplified web vitals reporting
const reportWebVitals = () => {
  // Web vitals reporting disabled to prevent chunk loading issues
  console.log("Web vitals reporting disabled");
};

export default reportWebVitals;
